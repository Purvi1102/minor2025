# Node modules (huge folder, regenerated by npm install)
node_modules/

# Environment variables (keep secrets safe)
.env

# Python notebook checkpoints
.ipynb_checkpoints/

# Pickle files (binary, system-specific, large)
*.pkl

# CSV datasets (optional — if it's large or sensitive)
*.csv

